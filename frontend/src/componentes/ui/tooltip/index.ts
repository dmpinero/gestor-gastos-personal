import {
  TooltipProvider as Provider,
  TooltipRoot as Root,
  TooltipTrigger as Trigger,
} from 'reka-ui'

export { default as TooltipContent } from './TooltipContent.vue'
export { Root as Tooltip, Provider as TooltipProvider, Trigger as TooltipTrigger }
